'use strict';

/**
 * it-service-first-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::it-service-first-section.it-service-first-section');
